# sagnis: repository instructions

Read `README.md`, then `CONTRIBUTING.md`. Before reporting any change:
`python core/sagnis.py --selftest` and `python core/sagnis.py build --root example/knowledge --check`.
The example is written with `save`, never by hand. Nothing here names a real person or business.
