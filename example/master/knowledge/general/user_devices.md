---
name: user_devices
title: "Devices"
description: "laptop (Windows, the master space lives there), phone (reads memory through the cloud session), no second machine; the two repos are private on GitHub"
metadata:
  type: user
---

One laptop holds `~/.claude` (the master space) and `~/Projects/Fernwood Landscaping`.
Both are private GitHub repositories. The phone uses a cloud session that clones both,
so anything not pushed from the laptop is invisible to it; see
[[feedback_push_same_turn]].
