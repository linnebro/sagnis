# Fernwood Landscaping — repository instructions

This repository is Maya Okafor's workspace. The assistant's index of it, its rules, task roster and decision log
lives in the AI workspace: read `~/.claude/knowledge/MEMORY.md` first, then
`~/.claude/knowledge/fernwood-landscaping/INDEX.md` (routes to one mirror index per
folder here), and the rules below. If the AI workspace travels with this repo as a
submodule (cloud sandboxes), the paths start `AI-Memory/` and an empty `AI-Memory/`
means `git submodule update --init`.

@~/.claude/knowledge/fernwood-landscaping/RULES.md
<!-- Keep this file to these lines. Anything longer belongs in the AI workspace:
the topic's rules file (read order, rules that have bitten) or a mirror index (what
a folder holds). -->
