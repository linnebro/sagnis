# Automations

Operator manual for every script in this folder: what it does, its schedule, inputs, outputs, gotchas. Update in the same commit as the script.

## Contents
- `build_route_sheet.py` — rebuilds `Routes/route-sheet.csv` from the QuickBooks export after the address join; run on a Tuesday evening after a new export lands
