# {{PROJECT}} — repository instructions

This repository is {{OWNER}}'s workspace. The assistant's index of it{{REPO_STATE}}
lives in the AI workspace: read `{{MASTER}}/knowledge/MEMORY.md` first, then
`{{MASTER}}/knowledge/{{PROJECT_SLUG}}/INDEX.md` (routes to one mirror index per
folder here){{RULES_LINE}}. If the AI workspace travels with this repo as a
submodule (cloud sandboxes), the paths start `AI-Memory/` and an empty `AI-Memory/`
means `git submodule update --init`.

{{RULES_IMPORT}}<!-- Keep this file to these lines. Anything longer belongs in the AI workspace:
the topic's rules file (read order, rules that have bitten) or a mirror index (what
a folder holds). -->
